# hyhdemo dev 分支代码评审报告

> 评审对象：`dev` 分支 commit `15908ad`（feat: 新增三种经典排序算法程序）
> 评审时间：2026-09-08 · Node v22.16.0 · 同机基准测试

## 评审结论

**正确性：通过 ✅**。固定用例（7 组 × 3 算法）与随机数据对照原生 `sort`（200 轮 × 3 算法）共 24 项断言全部通过，三种算法的排序结果均正确。

但性能与工程质量存在 **5 个问题**，其中 1 个为严重问题（会直接导致页面运行崩溃）。

---

## 💥 ISSUE-1 [严重] 快速排序对有序/重复输入直接导致栈溢出

- **位置**：`js/sorting.js:53-68`（`quickSort`，枢轴 `arr[right]` 在第 56 行）
- **现象**：枢轴固定取区间末尾元素 + 单向 Lomuto 分区。当输入已有序、逆序或元素全部相等时，每轮只能让枢轴归位一次，递归深度变成 O(n)。实测（Node v22.16.0）：

  | 输入 | 结果 |
  |------|------|
  | 10000 个随机数 | 1 ms，正常 |
  | **10000 个有序数** | **抛出 `RangeError: Maximum call stack size exceeded`** ❌ |
  | **20000 个有序数** | **同样栈溢出** ❌ |
  | **20000 个全相等数** | **栈溢出** ❌ |

- **影响**：博文演示页允许用户生成任意数据，一旦生成退化输入页面直接报错（演示逻辑没有任何错误兜底），博客文章的实用性受损。
- **修改建议**：
  1. 枢轴改为**三数取中**（左/中/右三个候选值的中位数），避免有序输入下枢轴总取极值；
  2. 分区改为**三路分区**（< 枢轴 / == 枢轴 / > 枢轴），重复元素一次性排到中间，同时把递归深度降为 O(log n)。

## ⚠️ ISSUE-2 [中等] 冒泡排序没有提前退出优化

- **位置**：`js/sorting.js:18-24`（内层循环无 `swapped` 标志）
- **现象**：即使某一轮没有任何交换（数组已经有序），外循环仍会走完全部 n-1 轮，已排序的输入依然是 O(n²)。实测（N=3000）：

  | 场景 | 冒泡排序 | 插入排序 |
  |------|---------|---------|
  | 有序输入 | 4 ms | **0 ms** |
  | 随机输入 | 4 ms | 1 ms |

  冒泡排序"有序 4ms ≈ 随机 4ms"，与插入排序"有序 0ms"形成鲜明对比——优化点被白白浪费。
- **影响**：数据基本有序时表现与最坏情况相同，唯一"简单且稳定"的招牌优势也没了。
- **修改建议**：每轮记录 `swapped` 标志，本轮未发生交换则 `break` 直接返回（最好情况降为 O(n)）。

## ⚠️ ISSUE-3 [轻微] 内层热循环使用解构赋值交换元素

- **位置**：`js/sorting.js:21`（冒泡内层）、`:60 / :64`（快速排序分区）
- **现象**：`[arr[j], arr[j+1]] = [arr[j+1], arr[j]]` 每次交换都会创建一个临时数组对象，冒泡排序的内层循环是最热路径，反复分配数组增大 GC 压力。
- **修改建议**：提取 `swap(arr, i, j)` 辅助函数，用普通临时变量交换；快速排序在 ISSUE-1 的改造中一并替换。

## ⚠️ ISSUE-4 [中等] 算法无模块导出，正确性无法自动化回归

- **位置**：`js/sorting.js` 全文件
- **现象**：算法函数直接挂在全局作用域，只能被演示页面调用。页面上的校验靠肉眼核对结果，代码改动后无法在命令行/CI 自动验证正确性。
- **影响**：算法这类"函数级逻辑"，无法回归测试等于每次改动都在裸奔。
- **修改建议**：加 `module.exports` 守卫（浏览器环境不受影响）+ 新增 `test/sorting.test.js`，用 Node 原生 `assert` 覆盖固定用例、200 轮随机对照、以及 ISSUE-1 的退化输入（大数组不崩溃）三个维度，`node test/sorting.test.js` 一键运行。

## ⚠️ ISSUE-5 [轻微] README 未同步新增文件

- **位置**：`README.md` 目录结构 / 功能章节
- **现象**：`js/sorting.js`、`posts/sorting-algorithms.html` 未列入目录结构，新读者照 README 无法对应实际文件布局。
- **修改建议**：补充目录结构说明与测试运行方式。

---

## 修复优先级建议

1. **ISSUE-1 优先修复**（运行崩溃，影响最大）
2. ISSUE-2 / ISSUE-4 次之（性能与可维护性）
3. ISSUE-3 / ISSUE-5 顺手修

（本报告后续将追加各问题的修复结果与修复后验证数据。）

---

## ✅ 修复结果（2026-09-08，修复后同机重测）

| # | 问题 | 修复方式 | 修复前 | 修复后 |
|---|------|---------|--------|--------|
| 1 | 快速排序栈溢出 | 枢轴改**三数取中** + **三路分区**（荷兰国旗） | 有序 10k/20k、全相等 20k 均 `RangeError` 崩溃 | 有序 10k **20ms**、有序 20k **54ms**、全相等 20k **0ms**（一次归位），不再崩溃 |
| 2 | 冒泡无提前退出 | 增加 `swapped` 标志，本轮无交换即 `break` | 有序 3k = 4ms | 有序 3k = **0ms**（最好情况 O(n)） |
| 3 | 解构交换临时数组 | 提取 `swap()` 辅助函数（快排使用）+ 冒泡热路径就地临时变量交换 | 每次交换分配临时数组 | 零临时数组分配；随机 3k 仍 **4ms**（注：最初直接用 `swap()` 函数时热路径增加函数调用开销达 46ms，遂改为内联临时变量） |
| 4 | 无模块导出、无法自动化验证 | 增加 `module.exports` 守卫 + 新增 `test/sorting.test.js` | 纯肉眼验证 | **630 项断言全部通过**（7 固定用例 + 600 随机对照 + 3 退化输入守卫 × 3 算法），`node test/sorting.test.js` 一键运行 |
| 5 | README 未同步 | 更新目录结构、补充排序测试说明、修正过时目录名 | 结构遗漏三个文件 | 与仓库实际结构一致 |

**修复全程验证**：修复后正确性 24/24 通过（与评审时同口径），退化输入回归测试通过，性能指标全部优于修复前或持平。

> 额外发现：ISSUE-3 的初版修复（统一改用 `swap()` 函数）让冒泡随机数据从 4ms 退化到 46ms，证实了热路径上函数调用开销大于解构分配的收益——最终方案在快排（O(n log n)，调用次数少）用函数、在冒泡热循环（O(n²)，调用次数多）用内联临时变量。
